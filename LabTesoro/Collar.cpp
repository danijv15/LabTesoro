#include "Collar.h"

#include <stdlib.h>
#include <time.h>

Collar::Collar() {

	int cantidad = 100 + (rand() % 201 - 100);
	int valor = 2000 + (rand() % 4001 - 2000);
	int peso = 500 + (rand() % 1001 - 500);

}

Collar::~Collar() {

}





